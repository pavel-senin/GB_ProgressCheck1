Begin
1) Создаем хранилище данныхCreate the list of lists
To fill list of lists Storage = []
2) проходим в цикле все элементы
if (i < Storage.size())
если длина меньше 4, тогда добавляем в итоговый список
if (Storage[i].size < 4)
3) выводим результат
NewStorage.add( Storage[i])